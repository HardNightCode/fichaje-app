Crear entorno virtual: 
    py -m venv venv

Activar el entorno virtual(dentro de raiz del proyecto):
 1:    Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser

 2:   venv\Scripts\Activate.ps1

Actualizar pip  
    python.exe -m pip install --upgrade pip

Instalar Flask
    pip install flask

Opcional pero recomendable, guarda las dependencias:
    pip freeze > requirements.txt

Ejecutar aplicacion
    py app.py



Aplicacion : /app.py 
    |
    |- Plantilla: /templates/index.html
    
sudo apt install -y libpango-1.0-0 libgdk-pixbuf2.0-0 libcairo2
sudo apt install -y libpangoft2-1.0-0
sudo apt-get install libpango1.0-0 libcairo2
sudo apt install -y libcairo2 libcairo2-dev libpango1.0-0 libpango1.0-dev libgdk-pixbuf2.0-0 libgdk-pixbuf2.0-dev
